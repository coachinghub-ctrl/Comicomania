# Las variables de la app web viven en apps/web/.env.local
# (Next.js las lee desde la carpeta de la app, no desde la raíz del monorepo).
# Copia apps/web/.env.example a apps/web/.env.local y complétalo.
